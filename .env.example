# Database Configuration
# PostgreSQL schema name (defaults to 'public' if not set)
# Use a unique schema name to isolate this instance from others in the same database
# Example: POSTGRES_SCHEMA=tenant_acme or POSTGRES_SCHEMA=app_prod
POSTGRES_SCHEMA=public

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
WORKSPACE_DOMAIN=yourcompany.com
