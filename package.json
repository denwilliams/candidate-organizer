{
  "name": "candidate-organizer-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "(cd frontend && npm install --include=dev && npm run build) && mkdir -p backend/static && cp -r frontend/out/* backend/static/"
  },
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  }
}
