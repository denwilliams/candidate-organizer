{
  "name": "candidate-organizer-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "(cd frontend && npm install --include=dev && npm run build && cp -r .next/static .next/standalone/.next/ && cp -r public .next/standalone/) && cp -r backend/cmd backend/internal backend/migrations . && chmod +x start-services.sh"
  },
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  }
}
