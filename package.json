{
  "name": "candidate-organizer-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "echo '=== Frontend contexts ===' && ls -la frontend/contexts && echo '=== Frontend lib ===' && ls -laR frontend/lib && npm install --prefix frontend && npm run build --prefix frontend && mkdir -p backend/static && cp -r frontend/out/* backend/static/"
  },
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  }
}
