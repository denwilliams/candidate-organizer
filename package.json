{
  "name": "candidate-organizer-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "(cd frontend && npm install --include=dev && npm run build) && mkdir -p backend/static && cp -r frontend/out/* backend/static/ && cp -r backend/cmd backend/internal . && ls -la cmd/"
  },
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  }
}
