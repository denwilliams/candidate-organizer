{
  "name": "candidate-organizer-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "(cd ../frontend && npm install --include=dev && npm run build && cp -r .next/static .next/standalone/.next/ && cp -r public .next/standalone/) && chmod +x ../start-services.sh"
  },
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  }
}
